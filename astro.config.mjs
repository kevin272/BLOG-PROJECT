import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/BLOG-PROJECT/', // Set the base path for GitHub Pages deployment
  site: 'https://kevin272.github.io/BLOG-PROJECT', // Optional: set the site URL
});
